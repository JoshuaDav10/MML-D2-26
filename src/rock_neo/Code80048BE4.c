#include "common.h"

/* func_80048BE4 — the whole body is `jr $ra; nop`, i.e. an empty function.
 * Most likely a stubbed-out hook the developers left in place, or a
 * table entry that has to exist but does nothing. */
void func_80048BE4(void) {
}
